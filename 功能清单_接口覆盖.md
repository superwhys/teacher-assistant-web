## src/views 功能清单（接口覆盖标记）

> 本文档基于项目源码整理：
> - 页面：`src/views/*.vue`
> - 路由：`src/routers/index.ts`
> - 后端接口封装：`src/api/*.ts`
>
> **标记规则**
> - **✅ 已有接口**：`src/api/` 已定义对应后端能力（不代表页面已接入）。
> - **❌ 无接口**：`src/api/` 未看到对应能力，需要补接口。
> - 如当前页面仍走 Pinia 本地 store，会额外注明“**当前未接入**”。

---

### 1) `src/api/` 接口模块总览

- **认证**：`src/api/auth.ts`
    - ✅ `/auth/register` 注册
    - ✅ `/auth/login` 登录
    - ✅ `/auth/send-code` 发送邮箱验证码
    - ✅ `/secret/verify` 授权码验证（返回 token）

- **班级**：`src/api/class.ts`
    - ✅ `/class/create` 创建班级
    - ✅ `/class/list` 班级列表
    - ✅ `/class/update/{classId}` 更新班级
    - ✅ `/class/delete/{classId}` 删除班级

- **学生 & 分组**：`src/api/student.ts`
    - ✅ `/student/create` 创建学生
    - ✅ `/student/delete/{studentId}` 删除学生
    - ✅ `/student/update/{studentId}` 更新学生
    - ✅ `/student/list` 学生列表
    - ✅ `/student/list/ungrouped` 未分组学生列表
    - ✅ `/student/group/create` 创建分组
    - ✅ `/student/group/delete/{groupId}` 删除分组
    - ✅ `/student/group/list?class_id=...` 分组列表
    - ✅ `/student/group/get` 获取分组详情
    - ✅ `/student/group/add_students` 分组添加成员
    - ✅ `/student/group/remove_students` 分组移除成员

- **积分（规则/加扣分/撤销/规则组）**：`src/api/points.ts`
    - ✅ `/points/rule/apply` 加扣分（apply）
    - ✅ `/points/rule/apply/undo/{applyId}` 撤销加扣分
    - ✅ `/points/rule/apply/list` 加扣分记录列表
    - ✅ `/points/rule/create|update|delete` 积分规则 CRUD
    - ✅ `/points/rule/group/create|update|delete|list` 规则分组 CRUD

- **积分商城**：`src/api/mall.ts`
    - ✅ `/mall/prizes/create|update|delete|list` 商品（奖品）CRUD
    - ✅ `/mall/prizes/records` 兑换记录列表
    - ✅ `/mall/prizes/exchange` 兑换
    - ✅ `/mall/prizes/undo-exchange` 撤销兑换

- **意见反馈**：`src/api/opinion.ts`
    - ✅ `/opinion/submit` 提交反馈

- **云端同步（计划移除）**：`src/api/cloud.ts`
    - ✅ `/cloud/sync` 同步
    - ✅ `/cloud/backups` 获取备份列表
    - ✅ `/cloud/backup` 获取备份内容

---

### 2) `src/views/` 页面功能清单（按路由）

#### `/auth`（`src/views/AuthView.vue`）
- **登录（邮箱+密码）**：✅（`authApi.login`）
- **注册（姓名/邮箱/密码/验证码）**：✅（`authApi.register`）
- **发送邮箱验证码**：✅（`authApi.sendEmailCode`）
- **登录态保存与路由重定向**：✅（依赖认证 token；逻辑在前端 store/router）

#### `/students`（`src/views/ClassView.vue`）
- **学生列表展示（卡片/列表 + 字母索引跳转）**：✅（可由 `studentApi.list` 支撑；**当前未接入**）
- **筛选/搜索/排序（姓名、按分组、按积分排序）**：❌（纯前端视图逻辑，无需接口；如需后端分页/排序/检索可再设计）

- **新增学生**
    - 单个添加：✅（`studentApi.create`；**当前未接入**）
    - 批量添加（文本拆分）：❌（未看到批量创建接口）
    - Excel 导入学生：❌（未看到导入/批量导入接口）
    - 下载学生 Excel 模板：❌

- **编辑学生（姓名/性别，改名同步分组成员）**：✅（`studentApi.update` + 分组成员同步；**当前未接入**）

- **删除学生（并从所有分组移除）**：✅（`studentApi.delete` + 分组成员移除；**当前未接入**）

- **分组管理（弹窗）**
    - 新建分组：✅（`studentApi.createGroup`；**当前未接入**）
    - 删除分组：✅（`studentApi.deleteGroup`；**当前未接入**）
    - 调整分组成员（保存成员列表）：✅（`studentApi.addStudentsToGroup/removeStudentsFromGroup`；**当前未接入**）
    - Excel 导入分组（含校验：不存在学生/一人多组/重复分组更新）：❌（未看到分组批量导入接口）
    - 下载分组导入模板：❌

- **学生积分展示（按学生展示总积分）**：✅（积分相关接口在 `pointsApi`；**当前未接入**）
- **学生统计弹窗（`StudentStatsDialog`）**：✅（可由 `pointsApi.listApplyRecords` 做统计；**当前未接入**）
- **学生表现报告（`StudentReportCard`）**：❌（未看到“报告生成/导出”接口）

#### `/points`（`src/views/PointsView.vue`）
- **学生积分列表（卡片/列表、批量选择）**：✅（积分能力在 `pointsApi`；**当前未接入**）
- **加分/扣分（单人/多人/全体）**：✅（`pointsApi.applyRule`；**当前未接入**）
- **撤回最近一次积分操作**：✅（`pointsApi.undoApply`；**当前未接入**）

- **排行榜**
    - 总榜（全部/周榜/月榜）：✅（可由 `pointsApi.listApplyRecords` 支撑；**当前未接入**）
    - 单项榜（按积分项）：✅（同上；**当前未接入**）

- **顶部菜单（`PointsHeaderActions`）**
    - 管理分值项：✅（可映射为 `pointsApi` 规则/规则组 CRUD；**当前未接入**）
    - 导入积分项（Excel）：❌（未看到积分项 Excel 导入接口）
    - 导入积分（Excel）：❌（未看到积分 Excel 导入接口）
    - 导出 Excel（最终积分/历史记录，支持分组/时间范围/按积分项筛选）：❌（本地导出，无接口）

#### `/points/history`（`src/views/PointsHistoryView.vue`）
- **积分记录列表（时间倒序）**：✅（`pointsApi.listApplyRecords`；**当前未接入**）
- **过滤（分组/加扣分/姓名关键词）**：✅（可由后端列表参数支持；当前为前端过滤）
- **撤回指定记录**：✅（`pointsApi.undoApply`；**当前未接入**）
- **清空班级积分记录**：❌（未看到清空接口）

#### `/points/shop`（`src/views/ShopView.vue`）
- **商品管理（新增/编辑/删除/列表）**：✅（`mallApi.createPrize/updatePrize/deletePrize/listPrizes`；**当前未接入**）
- **商品导入（Excel）**：❌（未看到商品 Excel 导入接口）
- **下载商品导入模板**：❌
- **兑换商品（扣可用积分 + 写兑换记录）**：✅（`mallApi.exchangePrize`；**当前未接入**）
- **兑换记录列表**：✅（`mallApi.listPrizeRecords`；**当前未接入**）
- **撤销兑换**：✅（`mallApi.undoExchangePrize`；**当前未接入**）

#### `/tools`（`src/views/ToolsView.vue`）
- **工具箱入口（计时器/点名器/抽奖器）**：❌（纯前端导航页）

#### `/tools/timer`（`src/views/TimerView.vue`）
- **计时器（预设/自定义/开始暂停/重置/进度条/时间到提示）**：❌（纯前端）

#### `/tools/rollcall`（`src/views/RollCallView.vue`）
- **点名随机抽取（支持按分组、是否不重复、历史列表、动效）**：✅（学生/分组数据可由 `studentApi` 支撑；**当前未接入**）
- **点名记录持久化**：❌（未看到点名记录接口）

#### `/tools/lottery`（`src/views/LotteryView.vue`）
- **奖池管理（新建/编辑/删除）**：❌（未看到抽奖器相关接口）
- **奖品管理（新增/编辑/删除/启用/权重）**：❌
- **从商城商品导入奖品**：✅（商城商品可由 `mallApi.listPrizes` 提供；但“导入到抽奖器”本身无专用接口）
- **抽奖/抽奖历史/清空历史**：❌

#### `/settings`（`src/views/SettingsView.vue`）
- **授权码验证（解锁功能/更新 token）**：✅（`authApi.verifySecret`）
- **锁屏（设置/清除锁屏密码、立即锁定）**：❌（未看到锁屏相关接口）
- **本地备份与恢复（JSON 导出/导入）**：❌（本地逻辑；你计划重构为全后端后会移除）

- **云端同步设置（计划移除）**：✅（`cloudApi.sync/getBackups/getBackup`）
    - 手动同步
    - 自动同步开关与间隔
    - 历史节点列表
    - 从节点恢复

#### `/opinion`（`src/views/OpinionView.vue`）
- **提交意见反馈**：✅（`submitOpinion` -> `/opinion/submit`）

---

### 3) 快速结论（给重构排期用）

- **已接入后端接口的功能**：认证（登录/注册/邮箱验证码/授权码验证）、意见反馈、云端同步（但你计划移除）。
- **接口已定义但页面尚未接入的核心模块**：班级、学生与分组、积分、积分商城。
- **接口明显缺口（src/api 未定义）**：Excel 导入/导出相关、点名/抽奖业务、锁屏/本地备份（如要保留为后端能力需新增接口，否则将被移除）。
